<div class="best plugin ever">
	Én most kiírom a változót amit kaptam. Ide: {{ adjective }}.<br>
	Ez meg egy include: {{> ./test/innertext }}
	<hr>
	Ez meg egy foreach:<br>
	<div id="comments">
		{{#each comments}}
			<h4><a href="/posts/{{../permalink}}#{{id}}">{{title}}</a></h4>
			<div>{{body}}</div>
		{{else}}
			<p class="empty">{{comment}}</p>
		{{/each}}
	</div>
</div>